'use client'

import TrainingData from '@/components/education/TrainingData'
import MathBackground from '@/components/backgrounds/MathBackground'

export default function Education() {
  return (
    <main className="relative min-h-screen">
      <MathBackground />
      <TrainingData />
    </main>
  )
}